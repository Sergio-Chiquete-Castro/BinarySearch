# CPlusPlus-BinarySearch

The C++ code implements a binary search algorithm to find a specific key within a sorted array and prints its index if found; otherwise, it indicates that the key is not found.
